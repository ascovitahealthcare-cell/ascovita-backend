{
  "name": "ascovita-backend",
  "version": "4.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```
→ **Commit**

### Step 3 — Delete all the new folders we added
Delete these folders from GitHub one by one:
```
middleware/    ← delete
models/        ← delete
migrations/    ← delete
seeders/       ← delete
routes/        ← delete
utils/         ← delete
config/        ← delete
.sequelizerc   ← delete
